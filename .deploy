{
    "profile": "oa",
    "artifacts": {
        "docker": [
            "{{project}}:task-{{gitsha}}",
            "{{project}}:api-{{gitsha}}"
        ],
        "lambda": [
            "openaddresses-lambdas/batch/{{gitsha}}.zip"
        ]
    }
}
