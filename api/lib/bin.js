'use strict';

/*
const tb = require('@mapbox/tilebelt');

const Err = require('./error');

class Bin {
    constructor(pool) {
        this.pool = pool;
    }

    map(job) {

    }

    tile(z, x, y) {

    }
}

module.exports = Bin;
*/
