{
    "type": "object",
    "required": [
        "job",
        "message"
    ],
    "additionalProperties": false,
    "properties": {
        "job": {
            "type": "integer",
            "description": "The Job ID of the error"
        },
        "message": {
            "type": "array",
            "description": "An Array of Error messages related to a given Job ID",
            "items": {
                "type": "string"
            }
        }
    }
}
